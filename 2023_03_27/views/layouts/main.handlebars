<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Engine</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        img {
            max-width: 100%;
        }
    </style>
</head>

<body>
    <header class="py-3">
        <h1 class="text-center">Welcome to the search engine!</h1>
        <div class="d-flex justify-content-center">
            <ul class="nav nav-pills">
                <li class="nav-item"><a href="/login" class="nav-link active" aria-current="page">Registered user</a></li>
                <li class="nav-item"><a href="/non-reg-request" class="nav-link">Not registered user</a></li>
                {{#if user}}
                    <li class="nav-item"><a href="/" class="nav-link">Atsijungti</a></li>
                {{/if}}
            </ul>
        </div>
        {{!-- {{#if user}}
            <div class="text-center pt-3">Sveiki, <strong>{{user.name}} {{user.last_name}}</strong></div>
        {{/if}} --}}
    </header>
    <div class="container">
        {{{body}}}
    </div>
</body>

</html>